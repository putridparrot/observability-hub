services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    image: dashboard-web:local
    container_name: dashboard-web
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
    restart: unless-stopped
