apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dashboard-web
  namespace: dashboard
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: dashboard-web
  endpoints:
    - port: http
      interval: 30s
      path: /
